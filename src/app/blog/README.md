#### BlogLayout
- We already know what layout is.
- In this layout file we are exporting a `const metadata` variable to generate static metadata.

#### Blog Component
- This component is a React Client Component i.e. 'use client' is mentioned at the top
- That means if you open browser devtools and go to network tab you will be able to see the `xhr/fetch` request is executing after the component renders in browser.

