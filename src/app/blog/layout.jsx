export const metadata = {
    title: "Sagar's Next.JS Blog",
    description:"Welcome to sagar's Next.JS Blog."
}
export default function BlogLayout({children}){
    return (
        <>
            {children}
        </>
    )
}